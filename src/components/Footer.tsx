import React from 'react';

/**
 * Simple footer component that displays branding. This is rendered at the
 * bottom of the application. Feel free to adjust typography or spacing
 * as required. It is intentionally minimalist to avoid distracting from
 * the drawing canvas.
 */
const Footer: React.FC = () => (
  <footer className="text-center py-2 text-sm text-gray-500 bg-gray-50 border-t border-gray-200">
    Made for Mentor Merlin
  </footer>
);

export default Footer;